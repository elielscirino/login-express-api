# login-express-api
Learning to build login systems

## To run

install dependencies:
```bash
npm i || npm install
```

.env file:
```bash
PORT = 3000
DB_HOST = localhost
DB_NAME = dbName
DB_USER = yourUser
DB_PASS = yourPassword

```
